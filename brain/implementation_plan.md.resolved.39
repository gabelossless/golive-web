# Implementation Plan: VibeStream Scaling & Creator Ecosystem

Transforming VibeStream into a hyper-personalized creator platform built for discovery and monetization.

## Proposed Changes

### [Backend] Database Evolution

We will expand the `profiles` table and introduce a `stats` layer for discovery.

#### [MODIFY] `profiles` table
- Add `banner_url` (TEXT): For custom channel branding.
- Add `social_links` (JSONB): To store Instagram, Twitter, and personal websites.
- Add `featured_video_id` (UUID): To showcase a specific video on the profile.
- Add `is_monetized` (BOOLEAN): To enable digital gifting and subscriptions.

### [Algorithm] Discovery & Personalization

#### [NEW] [vibe-rank.ts](file:///C:/GOLive/lib/vibe-rank.ts)
- **Technical Quality Score**: Boosts videos with higher resolution (1080p+), higher bitrates, and clear audio signals.
- **Engagement Velocity**: Tracks how quickly a video is gaining traction (views/hour).
- **Vibe-Rank Score**: [(Quality Score * 0.4) + (Engagement Velocity * 0.6)](file:///C:/GOLive/app/watch/%5Bid%5D/WatchClient.tsx#17-20).

#### [NEW] [personalization.ts](file:///C:/GOLive/lib/personalization.ts)
- **Interest Mapping**: Tracks categories (`Gaming`, `Music`, etc.) that users spend the most time watching.
- **"For You" Feed**: Re-ranks the home feed based on user interest scores and high-quality "Vibe-Rank" boosts.

### [Backend] Database & Agent Updates

#### [MODIFY] `videos` table
- Add `width` (INT), `height` (INT), `bitrate` (INT) for technical quality assessment.
- Add `quality_score` (FLOAT) for the aggregated QA-Sensor result.

#### [NEW] Discovery Agents
- **QA-Sensor**: Automatically scans newly processed videos to assign technical quality scores.
- **Topic Hunter**: Analyzes tag clusters across the platform to detect trending topics.

## Verification Plan

### Automated/Manual Tests
- **Branding Test**: Upload a banner and avatar, then verify they appear correctly on the public profile.
- **Velocity Test**: Boost a new video's views and verify it appears in the "Trending" section ahead of older, higher-view videos.
- **Social Link Test**: Click through social icons on a creator's profile.

### Budget Management ($1,000)
- **Infrastructure**: Optimize R2 caching and Supabase indexes to keep costs under $50/mo.
- **Growth**: Utilize the **Growth Momentum Engine** (bot interactions) to simulate initial traction without expensive ad spend.
